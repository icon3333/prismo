# Dev/test dependencies — not needed at runtime.
# Installs runtime deps too, so this is the only file needed to run the suite:
#   pip install -r requirements-dev.txt
-r requirements.txt
pytest~=9.1
