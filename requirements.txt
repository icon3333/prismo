# Pinned to compatible-release ranges (~=) so patch fixes flow in but a
# Docker rebuild can't silently pull a breaking major/minor.
# Versions = what the test suite runs green against. Bump deliberately.

# Flask and core dependencies
Flask~=3.1.3
Werkzeug~=3.1.8
Flask-Caching~=2.4.0

# Data processing
pandas~=3.0.3
numpy~=2.4.6

# Financial data
yfinance~=1.5.1
requests~=2.33.1

# Utilities
python-dotenv~=1.2.2
